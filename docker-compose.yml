services:
  shareflow:
    build: .
    container_name: shareflow
    ports:
      - "3000:3000"
    volumes:
      - clipboard_data:/data
    environment:
      - MAX_FILE_SIZE=10485760
      - MAX_TEXT_LENGTH=100000
      - PORT=3000
    restart: unless-stopped

volumes:
  clipboard_data:
